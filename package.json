{
  "name": "animation-ai-server",
  "version": "1.0.0",
  "description": "Backend server for the Roblox AI Animator Studio plugin. Receives rig/joint data and a text prompt, asks Claude to generate a keyframe animation plan, and returns it to the plugin.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5"
  }
}
